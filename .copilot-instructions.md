# Copilot instructions for this repository

Purpose
- This repository is a personal learning log / portfolio of front-end exercises (static HTML/CSS/JS). Each top-level folder is an independent mini-project or practice exercise.

Build, test, and lint
- No package.json, build system, test suite, or linters detected at the repository root.
- To run a specific exercise, open that exercise folder and open index.html in a browser (or use an editor Live Server extension).
- Quick static serve (optional):
  - Install a static server: npm install -g live-server
  - Start from a project folder: live-server .
- There are no test commands. If tests/CI are requested, propose a toolchain (e.g., Playwright/Jest + package.json) and get approval before introducing it.

High-level architecture
- This is a collection of independent, static front-end exercises:
  - Each exercise folder typically contains an index.html (entry), local CSS/JS files, and assets subfolders (images/, sounds/, etc.).
  - No shared build pipeline, no bundler, no centralized components — changes are localized to each folder.

Key conventions and repo-specific patterns
- Folder names frequently contain spaces (e.g., "tribute page", "Our menu"). Preserve spaces or normalize names if adding tooling.
- Entry file convention: index.html — assume this is the runnable target for Copilot requests that say "open/run this project." 
- Assets colocated in images/, sounds/, or images subfolders. When moving files, update relative references in HTML/CSS/JS.
- JS and CSS files are usually colocated (not imported as modules); avoid introducing absolute paths when editing.
- No package manager detected — avoid adding node-specific code unless the maintainer requests a unified toolchain.

Repository files scanned
- README.md (brief repo purpose).
- No CONTRIBUTING.md, no existing .github directory, and no AI assistant config files (CLAUDE.md, .cursorrules, AGENTS.md, .windsurfrules, CONVENTIONS.md, AIDER_CONVENTIONS.md, .clinerules) were found.

Guidance for Copilot sessions
- Default behavior for run/build tasks: treat projects as static sites (open index.html or serve folder with a static server).
- For edits: make minimal, local changes inside the target project's folder and preserve relative asset paths.
- For tests/CI or adding a build pipeline: request permission before changing repository layout or adding package.json; document proposed changes in CONTRIBUTING.md.

If you want these instructions placed in .github/copilot-instructions.md instead, enable creation of the .github directory (PowerShell Core 'pwsh' is required in this environment) and I can move it there.

---
Generated by Copilot assistant: summarized repo layout, run instructions, and Copilot-focused guidance.
